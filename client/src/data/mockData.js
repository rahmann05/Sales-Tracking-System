// @deprecated — Data telah dipecah per-domain (usersData, salesStopsData, teamsData, routesData, productsData).
// File ini hanya re-export untuk backward compatibility. Import dari '../data' (barrel) untuk kode baru.
export * from './index';
