import React from 'react';
import { DriverFieldView } from './DriverFieldView';

/**
 * DriverPage — Main workspace for Supir role.
 * Shows today's delivery route with stops and attendance.
 */
export const DriverPage = () => {
  return <DriverFieldView />;
};
